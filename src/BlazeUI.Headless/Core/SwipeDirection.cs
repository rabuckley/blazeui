namespace BlazeUI.Headless.Core;

/// <summary>
/// The direction of a swipe gesture used to dismiss or open a drawer.
/// Matches Base UI's <c>DrawerRoot.swipeDirection</c> values.
/// </summary>
public enum SwipeDirection
{
    Up,
    Down,
    Left,
    Right
}
