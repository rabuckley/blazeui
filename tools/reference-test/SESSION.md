- The `npm ci` command in `BlazeUI.Styled.Demo.csproj` fails in the pnpm workspace — it currently works only because `npm install` was run manually as a one-off fix. Consider switching to `pnpm install` or removing the npm target.
- `--tw-ring-color` CSS custom property still differs between reference and BlazeUI on some elements (10 real diffs remaining per component after theme variable fix). These are actual component styling differences, not theme configuration issues.
- Collapsible component (`collapsible.js`) has the same close animation issue that was fixed in accordion — it sets `hidden` immediately on close instead of deferring until the exit animation completes. Also still uses `ResizeObserver` to continuously update `--collapsible-panel-height` instead of switching to `auto` after animation.
